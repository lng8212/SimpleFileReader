<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleFileReader" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SimpleFileReader" parent="Base.Theme.SimpleFileReader">
        <item name="fontFamily">@font/opensans</item>
        <item name="popupMenuStyle">@style/PopupBackgroundColor</item>
        <item name="textAppearanceLargePopupMenu">@style/AppearanceLargePopupMenu</item>
    </style>

    <style name="PopupBackgroundColor" parent="@style/Widget.MaterialComponents.PopupMenu">
        <item name="android:popupBackground">@color/black</item>
    </style>

    <style name="AppearanceLargePopupMenu" parent="@style/TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>